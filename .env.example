# Django — used in hw_app/settings.py (placeholders only; rotate secrets for any shared environment)
DJANGO_SECRET_KEY=change-me-dev-only-not-for-production
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database URL for dj-database-url (example: local SQLite file in project root)
DATABASE_URL=sqlite:///db.sqlite3
